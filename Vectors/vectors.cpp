#include <bits/stdc++.h>
using namespace std;

int main() {
    // vector <int> vec;
    
    // vector<int> vec = {1, 2, 3};

    // vector <char> vec(3, 's');
    // cout << vec[0];
    // cout << vec[1];
    // cout << vec[2];

    vector <char> vec = {'a', 'b', 'c', 'd', 'e'};

    for(char val : vec) {
        cout << val << endl;
    }
}